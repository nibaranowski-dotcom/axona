{
  "$comment": "Project-scoped MCP servers for the Axona website. Put real tokens in env / settings.local.json, never here. shadcn registry MCP runs in-repo so it reads components.json (style radix-vega) + aliases. The dormant github + figma placeholders were removed (re-add with keys only if a story needs them).",
  "mcpServers": {
    "shadcn": {
      "command": "npx",
      "args": ["shadcn@latest", "mcp"]
    }
  },
  "$notesNextSteps": [
    "Analytics MCP (GA4/Plausible): add when conversion tracking lands (backlog CONV.2).",
    "CMS MCP (Sanity/Contentful/Notion): add when copy moves out of MDX into a CMS."
  ]
}
