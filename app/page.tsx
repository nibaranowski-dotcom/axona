// Deliberately neutral scaffold placeholder — no tokens, no accent, no hero.
// The real homepage (hero + sections) is built in HOME.1 and the HOME.* stories.
export default function Home() {
  return (
    <main className="flex min-h-dvh items-center justify-center p-8">
      <p>Axona — scaffold</p>
    </main>
  );
}
