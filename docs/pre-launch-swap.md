# Pre-launch swap list — Blueprint prototype (redesign/blueprint)

This is a **directional prototype** built to evaluate the Blueprint look. Per `design.md` principle 7
and `.claude/rules/content.md`, the items below are illustrative / placeholder and **must be replaced
or permissioned before the site goes live to real buyers**. Nothing here is a real, verified claim.

## Fabricated / illustrative stats (`content/blueprint.ts` → `stats`, rendered in the spec strip)
| Field | Prototype value | Status | Action before launch |
|---|---|---|---|
| Design partners | `3` | **illustrative** | Replace with the real signed count, or remove the cell. |
| Procurement cycle | `−60%` | **illustrative** | Replace with a measured figure from a design partner, or remove. |
| Robotics domains | `9` | scope-derived (from `content.md` §5 domains) | Verify against the final domain list; keep only if accurate. |
| Agent actions audited | `100%` | thesis claim (propose→approve→audit) | Keep only if the product guarantees it; otherwise soften/remove. |

**Rule:** no traction metric ships unless it is real and reconciles to `../memory/`. When in doubt,
remove the cell rather than soften a fabricated number.

## Placeholder logo wall (`content/blueprint.ts` → `logoWall`, rendered in the spec strip)
- Currently **striped placeholder tiles captioned "LOGO"** — no real company is named (avoids the
  trademark + implied-endorsement risk).
- **Before launch:** replace with **permissioned** real-customer logos (written sign-off per
  `../CLAUDE.md` — named partners require Nicolas's approval), or use generic/anonymized marks, or
  remove the wall entirely if there's nothing permissioned to show.

## Striped image placeholders (`.ph` blocks)
- Hero right column ("schematic — humans · machines · agents") and the logo tiles are `.ph`
  diagonal-stripe stand-ins for real art.
- **Before launch:** replace with designed robotics renders / schematics. Never ship a `.ph`
  placeholder (or a broken image) to production — gate on real art or a designed empty state.

## Module / industry copy (`content/blueprint.ts` → `platform`, `industries`)
- Hybrid copy: Axona's thesis spine (AI-native OS; agentic procurement wedge; humans + machines +
  agents; compounding memory/audit moat) expressed through the handoff's module/industry structure.
- Directionally accurate but **not the production copy pass** — reconcile every line to `../memory/`
  and `specs/content/content.md`, and confirm module names match the shipped product before launch.

## Brand
- Handoff said "Forge"; renamed to **Axona** (wordmark + square `A` mark) throughout. No "Forge"
  strings should remain.
